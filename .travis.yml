language: r
cache: packages

before_deploy:
  - Rscript -e 'remotes::install_cran("pkgdown")'

after_success:
  - Rscript -e 'pkgdown::build_site()'

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_PAT
  keep-history: true
  local-dir: docs
  on:
    branch: master
